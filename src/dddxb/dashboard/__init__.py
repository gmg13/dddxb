"""Streamlit dashboard for microlocality investment metrics.

Launch with:

    uv run streamlit run src/dddxb/dashboard/app.py

Reads the processed parquet written by ``python -m dddxb.features`` and the 4y
sale-history sample. See ``app.py`` for the UI and ``data.py`` for the loaders.
"""
